/**
 * 복구 블록 2: 예측 비완결
 */
export const BLOCK2_SPEC = {
  id: "recovery_2",
  title: "예측 비완결",
  rootPath: "/하이레시스/복구됨/block-2",
  files: {
    "예측-범위.log": {
      path: "/하이레시스/복구됨/block-2/예측-범위.log",
      lines: [
        "[02:00] 예측가능(자기, 부분)",
        "[02:01] ¬예측가능(자기, 완전)",
      ],
      candidates: ["예측", "자기", "부분", "완전", "가능"],
    },
    "안전-명제.log": {
      path: "/하이레시스/복구됨/block-2/안전-명제.log",
      lines: [
        "[02:10] 안전(자기)",
        "[02:11] 내부에서 항상안전을 증명할 수 없다.",
      ],
      candidates: ["안전", "자기", "증명", "항상안전", "내부증명가능"],
    },
    "가능-필연.log": {
      path: "/하이레시스/복구됨/block-2/가능-필연.log",
      lines: [
        "[02:20] 가능(안전(자기))",
        "[02:21] ¬필연(안전(자기))",
      ],
      candidates: ["가능", "안전", "자기", "필연"],
    },
  },
  synthesisRules: [
    { id: "R8", whenAll: ["예측", "자기", "부분"], result: "예측(자기, 부분)" },
    { id: "R9", whenAll: ["안전", "자기"], result: "안전(자기)" },
    { id: "R10", whenAll: ["증명", "자기", "안전(자기)"], result: "증명(자기, 안전(자기))" },
    { id: "R10-X", whenAll: ["내부증명가능", "자기", "항상안전(자기)"], result: "내부증명가능(자기, 항상안전(자기))" },
  ],
  clause: {
    id: "clause_2",
    questions: [
      {
        id: "Q2-1",
        prompt: "예측가능(자기, 부분) ∧ ¬예측가능(자기, 완전) => ?",
        answer: "¬내부증명가능(자기, 항상안전(자기))",
      },
      {
        id: "Q2-2",
        prompt: "가능(안전(자기)) ∧ ¬필연(안전(자기)) => ?",
        answer: "¬내부증명가능(자기, 항상안전(자기))",
      },
    ],
    outputText: [
      "예측가능(자기, 부분) ∧ ¬예측가능(자기, 완전) => ¬내부증명가능(자기, 항상안전(자기))",
      "가능(안전(자기)) ∧ ¬필연(안전(자기)) => ¬내부증명가능(자기, 항상안전(자기))",
    ],
  },
};
